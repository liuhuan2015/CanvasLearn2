package com.liuh.canvaslearn.activity;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

import com.liuh.canvaslearn.R;

/**
 * public void addArc (RectF oval, float startAngle, float sweepAngle)
 * <p>
 * 添加一个圆弧到path	直接添加一个圆弧到path中
 *
 *
 */
public class PathUseDetailAddArcActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_path_use_detail_addarc);
    }
}
